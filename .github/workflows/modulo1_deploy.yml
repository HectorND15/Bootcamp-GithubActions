# Yaml example of module1
name: Deploy Module 1

on:
  push:
    branches: [ modulo1 ]

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello world" 

  second_job:
    needs: first_job
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello world"
    

